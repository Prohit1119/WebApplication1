<script src="~/Scripts/jquery-3.7.0.min.js">

</script>
<script>
    function GetStudent() {
        $.ajax({
            url: "https://localhost:44335/Api/Student",
            type: 'get',
            datatype: 'json',
            success: BuildTable,
            error: DisplayError
        });
    }
    function BuildTable(Student) {
        alert(Student.Sid);
        $('#tblStudent tbody').empty();
        $.each(Student, function (index, Student) {
            
            var status = (Student.status) ? 'Active' : 'In-Active';
            $('#tblStudent').append('<tr><td align=center>' + Student.Sid + '</td><td>' + Student.Name +
                '</td><td align=right>' + Customer.Balance + '</td><td>' + Customer.City + '</td><td align=center>' + Status +
                '</td></tr>');

        });
    }

    function DisplayError() {
        window.alert("Error on the server, could not load the data.");
    }

</script>

<body onload="GetStudent()">
    <table width="100%">
        <tr>
            <td valign="top">
                <table border="1" align="center" id="tblStudent">
                    <caption>Customer Details</caption>
                    <thead>
                        <tr>
                            <th>Sid</th>
                            <th>Name</th>
                            <th>Class</th>
                            <th>Fees</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </td>
            
        </tr>
    </table>
</body>
