@model LoginModel
<html>
    <head>
        <title>Login Page</title>
        <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    </head>
    <body>
        <h1>Login Form</h1>
        <section>
          @*   @using(Html.BeginForm("Login","Account",FormMethod.Post,new{@class= "form-horizontal", role = "form" }))
          { 
        @Html.AntiForgeryToken();
        <hr />
           <div class="form-control">
            @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })


           </div>
           <div class="form-control">
            @Html.LabelFor(m => m.Password, new { @class = "col-md-2 control-label" })
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
           </div>
           <div class="form-control">
            @Html.CheckBoxFor(m => m.RememberMe, new { @class = "form-check-input" })
            @Html.LabelFor(m => m.RememberMe, new { @class = "form-check-label" })
           </div>
            <div class="form-control">
                <input type="submit" value="Log In" name="Submit" class="btn btn-secondary" />
                <input type="reset" value="Reset" name="Reset" class="btn btn-secondary" />


            </div>
            }
  *@       
  <form asp-controller="Account" asp-action="Login" role="form" class="form-horizontal">
      <hr/>
      <div class="form-control">
          <label asp-for="Email" class="col-md-2 control-lable"></label>
          <input asp-for="Email" class="col-md-2 control-input" />
      </div>
            
            <div class="form-group">
                <label asp-for="Password" class="col-md-2 control-label"></label>
                <input asp-for="Password" class="col-md-4 form-control" />
            </div>
            <div class="form-group">
                <input asp-for="RememberMe" />
                <label asp-for="RememberMe" class="col-md-2 control-label"></label>
            </div>
            <div class="form-group">
                <input type="submit" value="Log In" class="btn btn-primary" />
                <input type="reset" value="Reset" class="btn btn-primary" />
            </div>

  </form>
        </section>
    </body>
</html>